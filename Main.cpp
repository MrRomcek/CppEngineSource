#include <glad/glad.h>
#include <GLFW/glfw3.h>

#include <iostream>

using namespace std;




int main()
{
   
   /* GLFWmonitor* primary = glfwGetPrimaryMonitor();
    const GLFWvidmode& mod = *glfwGetVideoMode(primary);
    glfwSetWindowMonitor(window, primary, 0, 0,
        mod.width, mod.height, mod.refreshRate);*/
   
   
    return 0;
}

